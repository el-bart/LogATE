#!/bin/bash
set -e
DIR=$(dirname "$(readlink -e "$0")")
exec docker run \
       -u "$(id -u):$(id -g)" \
       -v "$DIR:/mnt/prj" \
       -w "/mnt/prj/build/clang6/Debug/address" \
       -it \
       --rm \
       "baszerr/logate-sdk"
